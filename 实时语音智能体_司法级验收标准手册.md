# ⚖️ [司法级验收手册 v2.0] Gemini Live 原生流式实时语音 Agent — 独立审计规范

> **审计员角色**：Antigravity (独立技术审计与验收 Agent)  
> **审计重点**：
> 1. **零 UI 跳页**：验证点击开启语音后，网页 100% 维持在原主界面，严禁跳出 `#live-voice-call-overlay` 弹窗。
> 2. **Gemini Live 原生流式**：验证双向 `/ws/voice/live` WebSocket 16kHz PCM 输入与 24kHz PCM 输出。

---

## 一、 司法级防造假与体验审查清单 (Checklist)

- [ ] **【零 UI 跳页审查】**：开启语音功能时，检查 DOM 元素中 `#live-voice-call-overlay` 样式必须为 `display: none`，所有交互在原主界面呈现。
- [ ] **【PCM 双向音频流审查】**：验证 WebSocket 传输数据包是否包含 `audio/pcm` 格式的 16kHz PCM 字节流。
- [ ] **【打断延时 $<50\text{ms}$】**：AI 播音时说话，前端 Web Audio Context 立即清空 Buffer 停止播音。

---

## 二、 性能与体验定量断言

| 验收维度 | 阈值标准 | 测试断言 (PyTest / Python) | 判定规则 |
|:---|:---|:---|:---|
| **UI 原界面维持率** | 100% 不跳转 | `assert is_modal_visible == False` | 出现弹窗直接否决 |
| **首包发声延迟** | $< 350\text{ms}$ | `assert latency_ms < 350` | $>350\text{ms}$ 判定为不流畅 |
| **打断响应速度** | $< 50\text{ms}$ | `assert interrupt_latency_ms < 50` | $>50\text{ms}$ 判定为打断受阻 |
