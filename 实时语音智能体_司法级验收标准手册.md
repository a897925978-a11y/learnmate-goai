# ⚖️ [司法级验收手册] 实时多语言语音智能体 — 独立审计与验收规范

> **审计员角色**：Antigravity (独立技术审计与验收 Agent)  
> **审计目标**：对写代码 Agent 交付的【实时多语言语音智能体】代码开展代码级防造假审查、性能指标量化断言与端到端全双工流程审计。

---

## 一、 司法级防造假与合规审查清单 (Anti-Fraud Checklist)

在验收时，审计员必须执行以下静态与动态代码扫描：

- [ ] **严禁假数据伪装**：代码中不得包含 `抱歉没听清`、`fake_audio`、`dummy_response` 等硬编码兜底逻辑；当 STT 或网络异常时必须抛出真实 Traceback。
- [ ] **严禁阻塞式同步调用**：主流程中不得包含阻塞主事件循环的 `time.sleep()`、同步 `requests.post()` 或同步 `run()` 调用。
- [ ] **严禁书面语大段灌水**：LLM 系统提示词中必须包含对输出字数（1-2句短句、30字以内）的强制约束，严禁输出书面大段课本文字。

---

## 二、 性能与体验定量验收断言 (Quantitative Performance Assertions)

| 验收维度 | 测试指标 / 阈值 | 审计断言代码 (PyTest / Python Script) | 判定规则 |
|:---|:---|:---|:---|
| **首包发声延迟** | 用户说话结束到听见第一声 $< 500\text{ms}$ | `assert first_byte_latency_ms < 500` | 超过 500ms 判定为“延迟不合规” |
| **打断响应速度** | 收到打断信号到音轨切断 $< 200\text{ms}$ | `assert interrupt_response_ms < 200` | 超过 200ms 判定为“打断顿卡” |
| **多语言匹配率** | 中/英/日/德/法 5 语种准确对答 $= 100\%$ | `assert detected_lang == response_lang` | 语种不匹配判定为“语言失效” |
| **回答口语化率** | 单次回答字数 $\le 45$ 个字符 (或 $< 25$ words) | `assert len(response_text) <= 45` | 超长吐字判定为“书面化” |

---

## 三、 自动化验收一键脚本 (`test_voice_agent_acceptance.py`)

审计员将在代码交付后自动运行以下测试脚本：

```python
import time
import requests
import asyncio
import websockets
import json

BASE_URL = "http://localhost:8000"
WS_URL = "ws://localhost:8000/ws/voice/stream"

def test_01_api_health_check():
    """验证 REST 与 WebSocket 接口联通性"""
    r = requests.get(f"{BASE_URL}/")
    assert r.status_code == 200, "主页/健康检查接口响应失败"
    print("✅ 1. 基础服务连通性通过")

def test_02_multilingual_short_response():
    """验证多语言短句口语化回答（中英日德法）"""
    test_cases = [
        ("Hello, help me with math!", "en-US"),
        ("こんにちは、数学を教えて", "ja-JP"),
        ("Guten Tag, kannst du helfen?", "de-DE"),
        ("Bonjour, comment ca va?", "fr-FR"),
        ("你好，今天数学课讲了什么？", "zh-CN")
    ]
    for text, expected_lang in test_cases:
        t0 = time.time()
        r = requests.post(f"{BASE_URL}/api/v1/voice/acoustic_chat", json={
            "student_id": "TEST-AUDIT",
            "voice_input_text": text,
            "selected_voice_key": "cute"
        })
        latency_ms = (time.time() - t0) * 1000
        assert r.status_code == 200, f"接口请求失败: {text}"
        data = r.json()
        
        # 1. 验证语种识别
        assert data["detected_language"] == expected_lang, f"语种匹配错误: 预期 {expected_lang}, 实际 {data['detected_language']}"
        # 2. 验证口语化短句（字符数 <= 60）
        assert len(data["ai_voice_response_text"]) <= 60, f"回答太长不像口语: {data['ai_voice_response_text']}"
        # 3. 验证音频 Base64 生成
        assert data["audio_data_url"] is not None and data["audio_data_url"].startswith("data:audio"), "音频 URL 缺失"
        print(f"✅ 多语言测试 [{expected_lang}] 通过！延迟: {latency_ms:.1f}ms, 回答: '{data['ai_voice_response_text']}'")

def test_03_barge_in_interrupt():
    """验证打断接口耗时 < 200ms"""
    t0 = time.time()
    r = requests.post(f"{BASE_URL}/api/v1/voice/interrupt")
    latency_ms = (time.time() - t0) * 1000
    assert r.status_code == 200
    assert latency_ms < 200, f"打断耗时过长: {latency_ms}ms"
    print(f"✅ 3. 打断信号响应通过！耗时: {latency_ms:.1f}ms")

if __name__ == "__main__":
    print("⚖️ 开始执行【实时多语言语音智能体】司法级独立验收测试...\n")
    test_01_api_health_check()
    test_02_multilingual_short_response()
    test_03_barge_in_interrupt()
    print("\n🎉 司法级独立验收全部 PASS！符合【实时语音智能体】标准！")
```
