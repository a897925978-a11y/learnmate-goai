# ⚖️ [司法级验收手册 v3.0] Qwen-Omni 原生流式实时语音 Agent — 独立审计规范

> **审计员角色**：Antigravity (独立技术审计与验收 Agent)  
> **审计重点**：
> 1. **Qwen-Omni 原生 Realtime WSS 协议**：验证后端连接的是 `wss://dashscope.aliyuncs.com/api-ws/v1/realtime` 且模型为 `qwen3.5-omni-flash`。
> 2. **零 UI 跳页**：验证点击开启语音后，网页 100% 维持在原主界面，严禁跳出全屏遮罩。

---

## 一、 静态与动态合规审查清单

- [ ] **【Qwen-Omni WSS 协议审查】**：代码中必须包含 `session.update`、`input_audio_buffer.append` 与 `response.audio.delta` 事件处理逻辑。
- [ ] **【零 UI 跳页审查】**：开启语音时，`#live-voice-call-overlay` 绝对不能显示，所有字幕打在主聊天框。
- [ ] **【双向 PCM 流审查】**：传输必须为 16kHz PCM 输入与 24kHz PCM 输出。

---

## 二、 定量断言

| 验收维度 | 阈值标准 | 判定规则 |
|:---|:---|:---|
| **协议原生度** | DashScope Realtime WSS | 采用普通 REST 轮询直接否决 |
| **首包发声延迟** | $< 350\text{ms}$ | $>350\text{ms}$ 判定为延时超标 |
| **主界面维持率** | 100% | 出现弹窗跳页直接否决 |
