# 🚀 [终极重构开发提示词 v3.0] Qwen-Omni 原生流式实时语音智能体

> **⚠️ 使用说明**：请直接将以下框内的完整提示词**无删减**复制发送给负责写代码的 Agent / 模型（如 Kimi / Claude / Subagent）。

---

```markdown
# 任务目标：重构打造【Qwen-Omni 原生流式实时语音 Agent】

你是一个顶级音视频架构师与 AI 智能体专家。请注意你的最高核心准则：

👉 **准则一：【针对通义千问 Qwen-Omni 世界模型 (`qwen3.5-omni-flash`) 专用打造！】**
放弃 HTTP POST 与传统 STT+TTS 串行级联模式！
直接接入 **阿里云 DashScope Realtime WebSocket 协议 (`wss://dashscope.aliyuncs.com/api-ws/v1/realtime`)**，实现原生 16kHz PCM 输入 $\leftrightarrow$ 24kHz PCM 输出的双向 Audio-to-Audio 流！

👉 **准则二：【零 UI 跳页，维持原界面！】**
彻底废除全屏黑色遮罩弹窗 (`#live-voice-call-overlay`)！
无论用户在做什么，开启语音后**页面绝对保持不变**。声波状态在顶栏原位闪烁，字幕与音频直接在主聊天框流式输出！

---

## 零、 强制执行协议 (Hard Enforcements)

1. **最大算力推理模式**：拒绝任何伪造界面、静态假弹窗与表面快捷方式。
2. **防偷懒防降级铁律**：严禁使用假音频数据或假定时器模拟对话；必须建立真实 WebSocket (`/ws/voice/omni_live`) 并对接 DashScope `qwen3.5-omni-flash` 原生 WSS Realtime API。
3. **自主饱和搜索协议**：在实现 DashScope `session.update`、`input_audio_buffer.append`、`response.audio.delta` 事件对接与 Web Audio PCM 采集时，**必须先主动搜索 DashScope Realtime WebSocket 官方文档**！

---

## 一、 3 大核心技术重构模块

### 1. 专用实时语音模块 (`backend/app/engine/qwen_omni_realtime_engine.py`)
- 新建该模块，负责与 DashScope `wss://dashscope.aliyuncs.com/api-ws/v1/realtime` 建立长连接。
- 下发 `session.update` 事件，配置 `modalities: ["text", "audio"]`, `voice: "zhi_xiaoban"`, `turn_detection: {"type": "server_vad"}`。
- 将前端发来的 `input_audio_buffer.append` 16kHz PCM 字节流推给 DashScope，并将 DashScope 返回的 `response.audio.delta` 24kHz PCM 实时推给前端播音。

### 2. 界面彻底解耦（维持原主界面 `index.html`）
- 禁用 `#live-voice-call-overlay` 弹窗。
- 点击麦克风后，顶栏显示：`🟢 Qwen-Omni 300ms 实时伴学中`。
- 识别字幕与 AI 口语回答直接追加至主聊天框 `#chat-box` 内。

### 3. 后端 WebSocket 路由 (`backend/app/main.py`)
- 挂载 `@app.websocket("/ws/voice/omni_live")` 双向代理路由。

---

## 二、 代码交付与自检

1. 必须修改/新建：
   - `backend/app/engine/qwen_omni_realtime_engine.py` (新增 Qwen-Omni Realtime WSS 模块)
   - `backend/app/main.py` (新增 `/ws/voice/omni_live` 端点)
   - `index.html` (维持原界面显示、PCM 采集与队列播音)
2. 提交前自检：
   - “是否完全基于 DashScope `qwen3.5-omni-flash` 原生 Realtime WSS 协议？”
   - “点击语音后是否保持在原主界面零跳转？”
```
