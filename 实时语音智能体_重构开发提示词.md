# 🚀 [终极开发提示词] 实时多语言语音智能体 (Real-Time Voice Agent)

> **⚠️ 使用说明**：请直接将以下框内的完整提示词**无删减**复制发送给负责写代码的 Agent / 模型（如 Kimi / Claude / Subagent）。该提示词已内置**最大算力推理指令、防偷懒降级禁令、自主饱和搜索协议与自检回路**。

---

```markdown
# 任务目标：重构打造【真实实时语音智能体 (Real-Time Multilingual Voice Agent)】

你是一个顶级音视频架构师与 AI 智能体专家。请注意你的最高核心准则：
👉 **【实时语音智能体 不是 问答机器人！】** 
严禁将项目写成“点一下按钮、等5秒钟、念一大段课本文字”的死板问答框！

---

## 零、 强制执行协议 (Hard Enforcements)

### 1. 最大算力推理模式 (Maximum Thinking Budget)
- **拒绝表面快捷方式与零发酵代码**。在编写任何代码前，必须进行深度逻辑拆解、状态机设计与网络/音频链路分析。
- 每一个模块的架构设计必须经过至少 5 重自检回路。

### 2. 反偷懒与防降级铁律 (Anti-Fraud & Zero Silence Fallback)
- **禁止伪造数据**：严禁在代码中写死“抱歉没听清”等假 response，严禁使用任何假 Mock 音频/文本数据。
- **禁止静默降级**：当 WebRTC/WebSocket/TTS/STT 失败时，必须诚实抛出精确的 Error Traceback 并引导修复，严禁静默切换到固定套话。
- **诚实失败 > 虚假成功**。

### 3. 自主饱和搜索协议 (Mandatory Saturated Search)
- 在遇到你不擅长或细节不确定的技术点时（例如：Web Audio API VAD 音量阈值算法、MediaRecorder 实时 AudioChunk 编码、WebSocket 字节流拆包、Edge-TTS 异步 Stream 分块传输、Gemini Live API 音频协议），**必须先主动调用搜索工具/查阅最新官方文档**，禁止凭空捏造 API 签名！

### 4. 提交前自检与对抗审查 (Self-Audit & Adversarial Check)
在交付代码前，你必须模拟红蓝对抗，主动问自己：
- “我的代码里是否有 >500ms 的阻塞式同步请求？”
- “当用户中途打断说话时，音频流是否能在 200ms 内瞬间切断？”
- “AI 回复是否控制在 1-2 句口语短句内？”
若存在上述问题，必须自我纠错重构后再交付代码！

---

## 一、 架构重构核心要求 (System Architecture)

彻底摒弃“STT 文本 $\to$ 大模型生成大段字 $\to$ 全文 TTS 朗读”的旧版低效串行架构，重构为 **全双工流式实时音视频交互架构**：

```
┌─────────────────────────────────────────────────────────────────────────┐
│               全双工实时语音智能体 (Real-Time Voice Agent)               │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  [用户麦克风] ──> 【Web Audio VAD 静音/说话检测】 ──> (无感自动开关)    │
│                           │ (实时 PCM/WebM 字节流)                      │
│                           ▼                                             │
│  [WebSocket 全双工通道] ──> 【多语言 STT & Gemini/Qwen 实时大模型】     │
│                           │ (首包 300ms 极速吐字 / 自动识别多国语言)    │
│                           ▼                                             │
│  [流式神经网络 TTS] ──> 【24kHz AudioStream 分包播放】 (声音抑扬顿挫)   │
│                           │                                             │
│  ⚡ [实时打断中枢] ──> 用户开口即刻在 200ms 内切断 AI 播音 (Barge-in)   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 二、 5 大核心功能模块具体规范

### 1. 麦克风 Web Audio VAD 无感检测 (取消按键)
- 放弃“点击按住/点击停止”的按键模式。
- 利用 Web Audio API (`AudioContext` + `ScriptProcessor` / `AudioWorklet`) 计算实时音量 RMS 能量与频域特征。
- 当说话能量超过动态门限（VAD Threshold）时自动开始捕获；连续静音 $>400\text{ms}$ 时自动判定一句话结束并发送。

### 2. WebSocket 全双工流式传输
- 前后端建立 WebSocket 长连接 (`/ws/voice/stream`)。
- 前端实时向后端 pipe 音频字节流；后端流式返回 `{"type": "transcript", ...}` 与 `{"type": "audio_chunk", "data": "base64..."}`。

### 3. 300ms 首包极速发声 (Audio First-Byte Latency)
- 后端 LLM 生成首句短文本时，立即并发提交 TTS 分包渲染。
- 保证前端从用户说话结束到听到 AI 第一声的**总延迟 $<500\text{ms}$**。

### 4. 纯口语化交谈与全球多语言自适应
- **LLM System Prompt 强制约束**：
  - 必须**100% 用用户使用的同种语言**对答（中/英/日/德/法/西/俄/韩/阿等）。
  - **单次回答严格限制在 1~2 句口语短句（15~30字以内）**，带有抑扬顿挫的交谈感，严禁吐大段书面课本文字！
  - 结尾保持适度开放性引导，把对话主动权交还给用户。

5. **全双工打断 (Barge-in) 与双端字幕/动画保留**
   - 当 AI 正在播音时，若 VAD 再次检测到用户说话，前端瞬间 `audio.pause()` 并在 200ms 内向后端发送打断信号。
   - 保持现有的高颜值 UI：沉浸式全屏通话字幕（实时显示 Transcript）与 3D/极光波形 Canvas 动态渲染。

---

## 三、 代码交付规范

1. 修改并完善：
   - `backend/app/engine/voice_engine.py` (全双工多语言流式引擎)
   - `backend/app/main.py` (WebSocket 路由与 REST 接口)
   - `index.html` (前端 VAD 检测、WebSocket 流式接包、Barge-in 极速打断)
2. 每一个函数必须附带清晰的 Docstring 与类型注解。
3. 编写完毕后，自动在控制台打印自检报告，确认零偷懒、零假数据！
```
