# 🚀 [终极重构开发提示词 v2.0] Gemini Live 原生流式实时语音智能体

> **⚠️ 使用说明**：请直接将以下框内的完整提示词**无删减**复制发送给负责写代码的 Agent / 模型（如 Kimi / Claude / Subagent）。

---

```markdown
# 任务目标：重构打造【Gemini Multimodal Live 双向原生流式实时语音 Agent】

你是一个顶级音视频架构师与 AI 智能体专家。请注意你的最高核心准则：

👉 **准则一：【零 UI 跳页，维持原界面！】**
彻底废除全屏黑色遮罩弹窗 (`#live-voice-call-overlay`)！
无论用户在做什么，开启语音后**页面绝对保持不变**。声波状态在顶栏原位闪烁，字幕与音频直接在主聊天框流式输出！

👉 **准则二：【不是问答机器人，是 Gemini Multimodal Live 实时语音 Agent！】**
摒弃低效串行（STT -> LLM -> TTS），采用业界最顶级的 **Google Gemini Multimodal Live API (BidiGenerateContent)** 原生音到音 (Audio-to-Audio) 双向流式架构！

---

## 零、 强制执行协议 (Hard Enforcements)

1. **最大算力推理模式**：拒绝任何伪造界面、静态假弹窗与表面快捷方式。
2. **防偷懒防降级铁律**：严禁使用假音频数据或假的定时器模拟对话；必须建立真实 WebSocket (`/ws/voice/live`) 并传输 16kHz 16-bit PCM 原始音频流。
3. **自主饱和搜索协议**：在实现 Web Audio API `AudioWorkletNode` PCM 采集、Float32 到 Int16 PCM 转换、Base64 音频分包传输与 Web Audio 队列播音时，**必须先主动搜索官方 Web Audio API 文档**！

---

## 一、 重构四大核心技术模块

### 1. 界面彻底解耦（维持原主界面）
- 在 `index.html` 中彻底移除/禁用 `#live-voice-call-overlay` 弹窗。
- 点击麦克风后，仅在主界面 Header 顶栏激活极简状态指示灯：`🟢 Gemini Live 300ms 实时伴学中`。
- 语音识别字幕与 AI 回答流直接实时追加至主聊天框 `#chat-box` 内。

### 2. 前端 16kHz PCM 流式采集与播放 (`index.html`)
- 使用 Web Audio API (`AudioContext`) 捕获麦克风 16000Hz 16-bit Mono PCM 字节流。
- 每 100ms 通过 `/ws/voice/live` WebSocket 端点向后端 pipe `realtime_input` 字节包。
- 收到服务端返回的 24kHz PCM 音频 Base64 时，使用 Web Audio API `AudioBufferSourceNode` 毫秒级拼接播音。

### 3. 后端 Gemini Live WSS 代理 (`backend/app/main.py` & `voice_live_engine.py`)
- 挂载 `/ws/voice/live` WebSocket 双向代理端点。
- 将前端 PCM 音频包转发给 Gemini Multimodal Live API (`GenerativeService.BidiGenerateContent`)；同时将 Gemini 生成的 24kHz PCM 与文字流实时 push 给前端。

### 4. 原生打断 (Barge-in)
- 利用 Gemini Live API 原生 Server VAD。当用户在 AI 播音时说话，后端接收到 `interrupted` 事件，前端瞬间清空播音 Buffer (耗时 $<50\text{ms}$)。

---

## 二、 代码交付与自检

1. 必须修改：
   - `index.html` (删除全屏弹窗、原界面 UI 整合、16kHz PCM 采集与播放)
   - `backend/app/engine/voice_live_engine.py` (新增 Gemini Live API WSS 引擎)
   - `backend/app/main.py` (新增 `/ws/voice/live` 路由)
2. 提交前进行自我对抗检查：
   - “点击语音后是否跳出了全屏弹窗？（若是，必须改正，维持原界面！）”
   - “说话时是否是双向 PCM 音频流传输？”
```
